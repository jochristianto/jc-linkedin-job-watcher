// Options page entry — watchlist, blocklists, scanning, retention, Telegram push
// (PRD §11 step 7, mockups/options.html).
//
// PREFACTOR (ticket 01): no behaviour yet. Defaults for every field come from the
// single source in types.ts (DEFAULT_SETTINGS).

console.log("[LJW] options page loaded");
