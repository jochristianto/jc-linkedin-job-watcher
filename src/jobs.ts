// Jobs page entry — the SAME list-view component mounted as a full tab (PRD §4),
// where notification clicks land.
//
// PREFACTOR (ticket 01): no behaviour yet. See mockups/render.ts for the tested
// shared component and mockups/README.md for the popup-vs-tab decision.

console.log("[LJW] jobs page loaded");
